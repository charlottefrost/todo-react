/**
 * Helpers: Utils.js
 * ------------------------------------------------------------------------------
 *
 */
export function capitialiseTitle(title) {
  return `${title.charAt(0).toUpperCase()}${title.slice(1)}`;
}
